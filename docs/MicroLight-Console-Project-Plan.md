# MicroLight Console 项目计划文档

## 1. 文档目的

本文档用于记录 `MicroLight Console` 的实施计划、阶段目标、任务拆解、依赖关系、里程碑和进度状态。

它服务于两个目标：

1. 把设计方案拆成可执行的开发步骤
2. 在项目推进过程中持续记录完成情况与下一步安排

关联设计文档：

- [MicroLight-Console-Design.md](D:\Aerlox\work\MicroLight Console\docs\MicroLight-Console-Design.md)

## 2. 项目目标

`MicroLight Console` 的目标是构建一个基于 `Electron + Node.js + Vue 3` 的本地微服务桌面控制台，用更低资源占用替代 IntelliJ IDEA 在“微服务构建、启动、查看日志、基础监测”场景下的使用。

首期项目范围聚焦：

- Maven 项目扫描
- Spring Boot 3.x 启动类识别
- 单服务构建与启动
- 服务停止与重启
- 实时日志查看
- 基础状态监测
- Windows `exe` 安装包发布

## 3. 计划原则

- 先打基础，再做增强功能
- 先保证“能稳定跑通”，再追求“更快、更聪明”
- 先完成单服务主链路，再做服务组编排
- 先支持 Maven 3.x 稳定路径，再扩展 Maven 4 / `mvnd`
- 计划按阶段推进，每阶段都要有可验证交付物

## 4. 状态定义

- `未开始`：尚未进入执行
- `进行中`：已开始，正在推进
- `已完成`：已经完成并通过当前阶段验收
- `阻塞中`：存在外部依赖或关键问题，无法继续推进
- `已延期`：暂不处理，后移到后续阶段

## 5. 当前总体进度

- 项目名称：`MicroLight Console`
- 当前阶段：`Phase 7 实现中`
- 当前状态：`进行中`
- 当前完成项：
  - 设计方案初稿已完成
  - Electron 桌面化方案已纳入设计
  - Maven 4 / `mvnd` 兼容策略已纳入设计
  - `Electron + Vue + Fastify + shared` monorepo 骨架已完成
  - 桌面端已能拉起本地后端并完成健康检查
  - Maven 项目扫描已完成
  - Spring Boot 启动类识别已完成
  - 运行环境探测已完成，支持 `java / mvnw / mvn / mvnd` 识别
  - 单服务构建、`java -jar` 启动、停止、重启主链路已完成
  - 实时日志流已完成
  - 日志落盘已完成
  - 基础进程监测已完成，包含 CPU / 内存采集
  - 端口识别与端口连通性检测已完成
  - 前端日志工作台第一版已完成
  - 前端已可查看服务状态、PID、构建器、端口、CPU、内存和日志文件路径
  - SQLite 持久化第一版已完成
  - 设置持久化第一版已完成
  - 最近项目记录与恢复第一版已完成
  - 服务启动偏好持久化第一版已完成
  - 服务启动参数、JVM 参数、Spring Profile 配置与回填已完成
  - 服务运行端口配置已接入真实启动参数
  - 桌面设置页已完成分组整理
  - 系统托盘与关闭窗口策略已完成
  - 项目级偏好持久化已完成，可恢复上次选中的服务
  - MVP 预检面板与后端预检接口已完成
  - Windows 打包原生依赖隔离已完成，避免 Electron ABI 重建污染根工作区
  - MVP 自动化验收脚本已完成，可通过 `npm run verify:mvp` 串联构建、打包和主链路验收
  - 应用内 Windows 发布与安装运行说明已完成，并纳入 MVP 自动化验收
  - 服务组顺序编排启动第一版已完成，支持按扫描顺序启动与停止最近服务组
  - 服务健康检查链路第一版已完成，支持端口连通与 `/actuator/health` 探测
  - 服务组持久化配置第一版已完成，支持保存、读取、删除和再次启动项目级服务组
  - 服务组启动间隔配置已完成，支持保存并按间隔顺序启动服务
  - 可配置健康检查路径已完成，支持单服务与服务组保存、回填和运行时探测
  - 服务依赖关系配置已完成，支持前端多选依赖、服务组持久化、拓扑排序和循环依赖拦截
  - `mvnd` 并行策略配置入口已完成，支持 Maven 线程参数配置、默认 `-T1` 保护模式和服务组持久化
  - Maven 4 实验性支持识别已完成，支持 Maven / `mvnd` 版本解析、兼容标记、环境展示和预检提示
- 当前下一步：
  - 补充 Maven 4 / `mvnd` 测试矩阵记录

## 6. 阶段总览

### Phase 0：方案冻结与工程准备

目标：

- 冻结 MVP 范围
- 确定技术栈
- 确定目录结构
- 明确开发规范与版本基线

交付物：

- 设计文档定稿
- 项目计划文档
- 初始化工程骨架
- 开发环境说明

状态：`已完成`

### Phase 1：桌面壳层与基础后端骨架

目标：

- 跑通 Electron 主进程、渲染进程、Node.js 后端三层结构
- 跑通桌面端启动本地后端服务
- 跑通前后端基础通信

交付物：

- Electron 基础窗口
- Vue 前端壳页面
- Fastify 后端基础服务
- 本地 `HTTP/WebSocket` 联通验证

状态：`已完成`

### Phase 2：项目扫描与服务识别

目标：

- 选择本地 Maven 工程目录
- 解析 `pom.xml`
- 扫描 Maven 模块
- 识别 Spring Boot 启动类

交付物：

- 项目选择功能
- 扫描结果列表
- 服务定义数据模型
- 扫描缓存机制

状态：`已完成`

### Phase 3：构建执行器与启动控制

目标：

- 跑通 `mvnw` / `mvn` / `mvnd` 识别
- 跑通单服务构建
- 跑通 `java -jar` 启动
- 跑通停止、重启

交付物：

- `BuildRunner` 抽象
- `MavenWrapperDriver`
- `SystemMavenDriver`
- `MvndDriver`
- `RuntimeLauncher`

状态：`已完成`

当前实现说明：

- 虽然代码中没有完全按文档命名为 `BuildRunner / MavenWrapperDriver / RuntimeLauncher`，但已经以等价结构实现：
  - `apps/server/src/runtime-tools.ts`
  - `apps/server/src/service-runtime.ts`
- 当前版本已经具备与上述交付物等价的能力边界：构建工具探测、构建命令选择、构建执行、jar 定位、Java 进程启动、停止、重启。

### Phase 4：日志与监测

目标：

- 实时采集服务日志
- 前端实时展示日志
- 落盘日志文件
- 采集服务基础状态

交付物：

- WebSocket 日志流
- 日志页签界面
- 基础进程监测
- 端口与 PID 状态展示

状态：`已完成`

当前实现说明：

- 已完成：
  - 实时日志流
  - 前端日志工作台第一版
  - PID 状态展示
  - 服务状态刷新
  - 日志落盘
  - CPU / 内存采集
  - 端口检测与展示
- 说明：
  - 文档中原定的 `WebSocket` 日志流，当前实际以 `SSE` 实现
  - 当前日志工作台已满足 Phase 4 目标，后续仍可在 Phase 5 / Phase 6 继续增强交互体验

### Phase 5：配置持久化与桌面体验完善

目标：

- 保存项目配置
- 保存服务定义
- 保存最近项目
- 完善窗口行为与设置页

交付物：

- SQLite 配置存储
- 设置页
- 最近项目列表
- 目录选择器
- 托盘和关闭行为策略

状态：`已完成`

当前实现说明：

- 已完成：
  - SQLite 持久化基础设施
  - 应用设置保存与读取
  - 最近项目记录与恢复
  - 服务启动偏好保存与回填
  - 前端设置区第一版
  - 服务启动参数、JVM 参数、Spring Profile 配置面板
  - 服务级启动参数持久化与重新扫描回填
  - 运行端口已映射到真实 Spring Boot 启动参数
  - 设置页分组与桌面行为配置
  - 系统托盘与关闭窗口策略
  - 项目级偏好持久化与恢复上次选中服务
- 后续可增强：
  - 项目表 / 服务表 / 实例表 / 分组表等更细数据模型

### Phase 6：MVP 集成验收与 Windows 发布

目标：

- 完成 MVP 联调
- 验证典型项目可用性
- 产出 Windows 安装包

交付物：

- MVP 验收记录
- 已知问题清单
- `electron-builder` 打包产物
- 首个 Windows `exe` / 安装器版本

状态：`已完成`

当前实现说明：

- 已完成：
  - 嵌套多模块 Maven 项目递归扫描
  - 多模块构建命令日志输出
  - 常见 Maven 构建失败场景的错误提示补强
  - 新增嵌套多模块 Spring Boot 验证夹具
  - `electron-builder` 打包配置
  - 生产态桌面端内置后端启动链路
  - Windows 安装包产物生成
  - `win-unpacked` 运行验证
  - MVP 预检接口与前端预检面板
  - 打包 staging 目录已移出 monorepo，`better-sqlite3` 的 Electron ABI 重建仅发生在隔离目录
  - 打包后开发态 Node 服务端可继续加载根工作区 `better-sqlite3` 并返回 `/api/health`
  - MVP 自动化验收脚本，覆盖健康检查、单模块扫描、嵌套多模块扫描、预检接口、中文和空格路径扫描、Windows 发布产物检查
  - 应用内 Windows 发布与安装运行说明，覆盖安装步骤、运行验证步骤和发布产物状态

### Phase 7：增强能力

目标：

- 服务组编排启动
- 健康检查
- Maven 4 实验性支持验证
- `mvnd` 性能模式完善

交付物：

- 服务组启动
- 健康检查策略
- Maven 4 兼容性测试记录
- `mvnd` 兼容与配置页面

状态：`进行中`

当前实现说明：

- 已完成：
  - 服务组运行时管理器第一版
  - 服务组顺序启动接口
  - 服务组停止接口
  - 前端扫描结果一键顺序启动服务组
  - 前端最近服务组状态展示
  - 服务组接口契约已纳入 MVP 自动化验收
  - 运行中服务健康检查字段与前端展示
  - 项目级服务组持久化，支持保存当前扫描服务组、读取已保存服务组、删除已保存服务组
  - 服务组启动间隔配置，支持前端输入、持久化和运行时等待
  - 可配置健康检查路径，支持前端配置、默认值回填、服务偏好持久化、服务组持久化和运行时探测
  - 服务依赖关系配置，支持按依赖拓扑顺序启动、循环依赖错误提示和服务组依赖持久化
  - `mvnd` 并行策略配置入口，支持 Maven 线程参数输入、默认保护模式和构建命令参数验收
  - Maven 4 实验性支持识别，支持版本解析、实验性标签、`mvnd 1.x/2.x` 目标 Maven 标记和预检兼容提示
- 尚未完成：
  - Maven 4 / `mvnd` 测试矩阵记录

## 7. 详细任务拆解

## 7.1 Phase 0：方案冻结与工程准备

任务清单：

- 明确 MVP 功能边界
- 确认 Node.js / Electron / Vue / TypeScript 版本
- 确定 monorepo 或多包目录结构
- 确定代码规范、Lint、Formatter、提交规范
- 确定开发态与发布态启动方式
- 确定配置文件、数据库、日志的落盘目录

建议产物：

- `docs/MVP-Feature-List.md`
- `docs/Architecture-Modules.md`
- 项目初始化脚手架

## 7.2 Phase 1：桌面壳层与基础后端骨架

任务清单：

- 初始化 Electron 主进程
- 初始化 Vue 渲染进程
- 初始化 Fastify 后端
- 建立开发态一键启动方式
- 建立 Electron 拉起本地后端的机制
- 建立健康探针接口，如 `/api/health`
- 建立基础日志输出机制

验收标准：

- 打开桌面应用后能看到主窗口
- 后端可随应用启动
- 前端可请求后端接口
- 后端日志可定位

## 7.3 Phase 2：项目扫描与服务识别

任务清单：

- 实现目录选择器
- 实现项目路径校验
- 解析根 `pom.xml`
- 识别聚合模块
- 扫描 `src/main/java`
- 检测 `@SpringBootApplication` + `main` 方法
- 构建统一服务定义对象
- 保存扫描结果缓存

验收标准：

- 能识别常见 Maven 单模块项目
- 能识别常见 Maven 多模块项目
- 能列出候选启动类
- 多启动类模块能被标记

## 7.4 Phase 3：构建执行器与启动控制

任务清单：

- 设计 `BuildRunner` 接口
- 实现 `mvnw` 驱动
- 实现 `mvn` 驱动
- 实现 `mvnd` 驱动
- 实现 Maven 版本探测
- 实现 `mvnd` 版本探测
- 生成标准构建命令模板
- 识别构建产物 jar
- 实现 `java -jar` 启动
- 实现 PID 记录
- 实现停止与重启

验收标准：

- 能对单服务执行构建
- 能启动 jar 并保持进程存活
- 能停止与重启服务
- 能识别构建失败并反馈
- 能区分 `mvnw` / `mvn` / `mvnd`

当前结论：

- 已完成并通过当前阶段验收
- 已在本机环境完成实际验证：
  - `mvnw / mvn / mvnd` 探测可用
  - 示例 Spring Boot 服务可构建
  - 示例服务可启动、停止、重启
  - 构建失败时可返回错误信息

## 7.5 Phase 4：日志与监测

任务清单：

- 捕获子进程 `stdout/stderr`
- 建立日志广播总线
- 建立 WebSocket 推送
- 构建日志页签 UI
- 增加日志过滤与暂停滚动
- 实现日志落盘
- 实现基础 CPU / 内存采集
- 实现端口连通性检查

验收标准：

- 服务启动后日志能实时显示
- 日志不会因高频输出立即卡死界面
- 进程状态与资源占用可见

当前结论：

- 已完成并通过当前阶段验收
- 已满足：
  - 服务启动后日志可实时显示
  - 日志已支持落盘
  - 前端可以看到服务状态、PID、端口、CPU、内存
  - 已具备 SSE 实时流能力
  - 已具备独立日志工作台第一版

## 7.6 Phase 5：配置持久化与桌面体验完善

任务清单：

- 初始化 SQLite
- 设计项目表、服务表、实例表、分组表
- 保存最近打开项目
- 保存服务默认参数
- 保存构建驱动偏好
- 完成设置页
- 增加关闭窗口策略
- 增加托盘支持

验收标准：

- 关闭后再次打开仍可恢复项目配置
- 设置修改可持久化
- 桌面端行为符合预期

当前结论：

- 已完成并通过当前阶段验收
- 已满足：
  - 设置修改可持久化
  - 最近项目可恢复
  - 服务构建偏好可回填
  - 服务启动参数、JVM 参数、Spring Profile 可配置并回填
  - 运行端口配置会真实作用于服务启动
  - 更完整的设置页与桌面行为策略
  - 项目级偏好持久化与恢复

## 7.7 Phase 6：MVP 集成验收与 Windows 发布

任务清单：

- 完整串联用户主流程
- 验证常见 Spring Boot 3.x 项目
- 验证多模块项目
- 验证 Windows 路径和中文路径
- 配置 `electron-builder`
- 生成安装包
- 编写安装与使用说明

验收标准：

- 从打开项目到启动服务、查看日志全链路可用
- Windows 安装包可正常安装与运行
- 核心异常场景有基本提示

当前结论：

- 已完成并通过当前阶段验收
- 已完成：
  - 嵌套多模块项目可递归扫描
  - 多模块服务可通过 `-pl ... -am` 完成构建与启动
  - 常见 Maven 失败场景已经有更清晰的错误提示
- 已完成实际验证：
  - 嵌套多模块夹具可成功启动并访问接口
  - 错误模块路径会返回可读的 reactor 提示
  - `electron-builder` 已生成 Windows 安装包
  - `win-unpacked` 可执行文件启动后，内置后端可正常提供 `/api/health`
  - 预检接口可返回数据库、Java、Maven、模块扫描和启动类识别结果
  - Windows 打包 staging 目录已隔离到仓库外，`electron-builder` 不再将 monorepo 根目录识别为应用工作区
  - 隔离打包后，开发态服务端可通过 Node 运行并返回 `200 true`
  - `npm run verify:mvp` 可完成构建、Windows 打包和 MVP 自动化验收
  - 应用内发布面板可展示 Windows 安装步骤、运行验证步骤和发布产物状态

## 7.8 Phase 7：增强能力

任务清单：

- 服务组编排启动
- 健康检查链路
- 历史日志检索
- Maven 4 实验性支持验证
- `mvnd` 兼容与并行策略优化
- `-T1` 保护模式
- Maven 4 / `mvnd 2.x` 测试矩阵

验收标准：

- 支持多服务顺序启动
- 支持实验性 Maven 4 项目验证
- `mvnd` 可作为可控选项使用

当前结论：

- 已启动，处于进行中
- 已完成：
  - 后端服务组编排接口第一版
  - 前端可将当前扫描出的服务按模块顺序组成临时服务组并启动
  - 前端可停止最近一次服务组
  - 空服务组保护已纳入自动化验收
  - 服务运行状态包含健康检查状态、健康地址、检查详情和最近检查时间
  - 服务组保存、读取和删除链路已纳入自动化验收
  - 服务组启动间隔保存链路已纳入自动化验收
  - 健康检查路径支持按服务配置，并已覆盖默认路径、自定义路径和服务组保存验收
  - 服务依赖拓扑排序和循环依赖识别已纳入自动化验收
  - Maven 线程参数生成与服务组持久化已纳入自动化验收
  - Maven 4 与 `mvnd 2.x` 版本解析、兼容识别与预检提示已纳入自动化验收
- 尚未满足：
  - Maven 4 / `mvnd` 测试矩阵

## 8. 关键里程碑

### 里程碑 M1：完成技术底座

标志：

- Electron + Vue + Fastify 骨架跑通
- 本地通信链路稳定

### 里程碑 M2：完成项目扫描

标志：

- 能打开并识别 Maven 项目与 Spring Boot 启动类

### 里程碑 M3：完成单服务主链路

标志：

- 能构建、启动、停止、重启、看日志

### 里程碑 M4：完成 MVP

标志：

- 用户可用桌面端替代 IDEA 完成基础联调启动工作

### 里程碑 M5：完成首个发布版

标志：

- 可生成并分发 Windows 安装包

## 9. 关键依赖与风险关注

关键依赖：

- Node.js 运行环境
- Electron 打包链路
- Java 17+
- Maven 3.x / 4.x 运行环境
- `mvnd` 可用性

关键风险：

- Maven 多模块复杂结构导致扫描异常
- `mvnd` 与特定项目插件不兼容
- Electron 打包后路径、权限、杀毒软件影响
- 日志高吞吐导致渲染进程卡顿

## 10. 进度记录模板

后续推进时建议按下面格式持续更新：

### 本周状态

- 当前阶段：
- 当前任务：
- 完成内容：
- 遇到问题：
- 下周计划：

### 任务状态模板

- 任务名称：
- 所属阶段：
- 负责人：
- 状态：`未开始 / 进行中 / 已完成 / 阻塞中 / 已延期`
- 开始日期：
- 完成日期：
- 备注：

## 11. 当前建议的下一步

建议按以下顺序继续推进：

1. 启动 Phase 7 服务组编排数据模型与接口设计
2. 增加服务组顺序启动、停止和失败中断策略
3. 补充服务健康检查链路
4. 开始 Maven 4 实验性兼容验证
5. 继续完善 `mvnd` 并行构建策略与配置入口
